name: riveFlutterBlog
description: An example of animating .riv files using Flutter
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rive: 
    path: ../../rive/rive-flutter

flutter:
  uses-material-design: true
  assets:
    - assets/truck.riv